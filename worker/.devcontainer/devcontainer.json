{
  "name": "SIPULAU Worker",
  "image": "osgeo/gdal:ubuntu-small-3.4.0",
  "settings": {
    "python.pythonPath": "/usr/bin/python",
    "python.formatting.provider": "black",
    "python.formatting.blackPath": "black"
  },
  "extensions": ["ms-python.python"],
  "postCreateCommand": "apt update && apt install python3-pip -y && pip3 install --upgrade pip && pip3 install -r requirements.txt black",
  "runArgs": ["--network=sipulaudev"]
}
